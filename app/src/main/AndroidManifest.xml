<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        package="com.avinash">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:name=".CoreApplication"
            tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".activities.SplashActivity" android:noHistory="true" android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activities.name.NameActivity"/>
        <activity android:name=".activities.firstquestion.FirstQuestionActivity"/>
        <activity android:name=".activities.secondquestion.SecondQuestionActivity"/>
        <activity android:name=".activities.summary.SummaryActivity"/>
        <activity android:name=".activities.history.HistoryActivity"/>

    </application>

</manifest>